# frontend/public/

Static files that don't get processed by the build tool:
- favicon.ico
- index.html (or Vite's index.html at project root, depending on setup)
- robots.txt, manifest.json (optional)
