# src/hooks/

Custom React hooks, e.g.:
- useAuth()          -> access current user/role easily in any component
- useOrders()        -> subscribe to live order status updates (Firebase Realtime)
- useTableStatus()   -> subscribe to live table green/red status
