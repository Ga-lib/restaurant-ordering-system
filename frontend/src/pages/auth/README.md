# pages/auth/

Login / Signup pages, shared by all roles (role is checked after login
to route the user to the correct dashboard: customer, admin, kitchen, waiter, rider).

Uses Firebase Authentication.
