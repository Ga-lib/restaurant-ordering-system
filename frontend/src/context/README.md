# src/context/

React Context providers for global state, e.g.:
- AuthContext.jsx -> current logged-in user + role (customer/admin/kitchen/waiter/rider)
- CartContext.jsx -> customer's current order/cart before checkout
