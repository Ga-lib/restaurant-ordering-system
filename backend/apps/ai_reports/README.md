# apps/ai_reports/

Handles AI-powered Reports & Analytics using the Grok free API.

Will contain:
- Sales reports, profit/loss reports (Tk)
- Most popular food prediction
- Customer behavior analysis
- Grok API call wrapper (kept non-blocking — app must still work if Grok API is down/rate-limited)
